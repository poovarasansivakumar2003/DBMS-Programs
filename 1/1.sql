-- Create Table for Publisher
CREATE TABLE PUBLISHER (
    NAME VARCHAR(50) PRIMARY KEY,
    PHONE BIGINT,
    ADDRESS VARCHAR(100)
);

-- Create Table for Book
CREATE TABLE BOOK (
    BOOK_ID INT PRIMARY KEY,
    TITLE VARCHAR(100),
    PUB_YEAR YEAR,
    PUBLISHER_NAME VARCHAR(50),
    FOREIGN KEY (PUBLISHER_NAME) REFERENCES PUBLISHER(NAME) ON DELETE CASCADE
);

-- Create Table for Book Authors
CREATE TABLE BOOK_AUTHORS (
    BOOK_ID INT,
    AUTHOR_NAME VARCHAR(100),
    PRIMARY KEY (BOOK_ID, AUTHOR_NAME),
    FOREIGN KEY (BOOK_ID) REFERENCES BOOK(BOOK_ID) ON DELETE CASCADE
);

-- Create Table for Library Branch
CREATE TABLE LIBRARY_BRANCH (
    BRANCH_ID INT PRIMARY KEY,
    BRANCH_NAME VARCHAR(100),
    ADDRESS VARCHAR(100)
);

-- Create Table for Book Copies
CREATE TABLE BOOK_COPIES (
    BOOK_ID INT,
    BRANCH_ID INT,
    NO_OF_COPIES INT,
    PRIMARY KEY (BOOK_ID, BRANCH_ID),
    FOREIGN KEY (BOOK_ID) REFERENCES BOOK(BOOK_ID) ON DELETE CASCADE,
    FOREIGN KEY (BRANCH_ID) REFERENCES LIBRARY_BRANCH(BRANCH_ID) ON DELETE CASCADE
);

-- Create Table for Card
CREATE TABLE CARD (
    CARD_NO INT PRIMARY KEY
);

-- Create Table for Book Lending
CREATE TABLE BOOK_LENDING (
    BOOK_ID INT,
    BRANCH_ID INT,
    CARD_NO INT,
    DATE_OUT DATE,
    DUE_DATE DATE,
    PRIMARY KEY (BOOK_ID, BRANCH_ID, CARD_NO),
    FOREIGN KEY (BOOK_ID) REFERENCES BOOK(BOOK_ID) ON DELETE CASCADE,
    FOREIGN KEY (BRANCH_ID) REFERENCES LIBRARY_BRANCH(BRANCH_ID) ON DELETE CASCADE,
    FOREIGN KEY (CARD_NO) REFERENCES CARD(CARD_NO) ON DELETE CASCADE
);

-- Insert Data into Publisher Table
INSERT INTO PUBLISHER VALUES 
('MCGRAW-HILL', 9989076587, 'BANGALORE'),
('PEARSON', 9889076565, 'NEW DELHI'),
('RANDOM HOUSE', 7455679345, 'HYDERABAD'),
('HACHETTE LIVRE', 8970862340, 'CHENNAI'),
('GRUPO PLANETA', 7756120238, 'BANGALORE');

-- Insert Data into Book Table
INSERT INTO BOOK VALUES 
(1, 'DBMS', 2017, 'MCGRAW-HILL'),
(2, 'ADBMS', 2016, 'MCGRAW-HILL'),
(3, 'CN', 2016, 'PEARSON'),
(4, 'CG', 2015, 'GRUPO PLANETA'),
(5, 'OS', 2016, 'PEARSON');

-- Insert Data into Book Authors Table
INSERT INTO BOOK_AUTHORS VALUES 
(1, 'NAVATHE'),
(2, 'NAVATHE'),
(3, 'TANENBAUM'),
(4, 'EDWARD ANGEL'),
(5, 'GALVIN');

-- Insert Data into Library Branch Table
INSERT INTO LIBRARY_BRANCH VALUES 
(10, 'RR NAGAR', 'BANGALORE'),
(11, 'RNSIT', 'BANGALORE'),
(12, 'RAJAJI NAGAR', 'BANGALORE'),
(13, 'NITTE', 'MANGALORE'),
(14, 'MANIPAL', 'UDUPI');

-- Insert Data into Book Copies Table
INSERT INTO BOOK_COPIES VALUES 
(1, 10, 10),
(1, 11, 5),
(2, 12, 2),
(2, 13, 5),
(3, 14, 7),
(5, 10, 1),
(4, 11, 3);

-- Insert Data into Card Table
INSERT INTO CARD VALUES (100), (101), (102), (103), (104);

-- Insert Data into Book Lending Table
INSERT INTO BOOK_LENDING VALUES 
(1, 10, 101, '2017-01-01', '2017-06-01'),
(3, 14, 101, '2017-01-11', '2017-03-11'),
(2, 13, 101, '2017-02-21', '2017-04-21'),
(4, 11, 101, '2017-03-15', '2017-07-15'),
(1, 11, 104, '2017-04-12', '2017-05-12');

-- 1. Retrieve details of all books in the library – id, title, name of publisher, authors, number of copies in each branch, etc.
SELECT 
    B.BOOK_ID, 
    B.TITLE, 
    B.PUBLISHER_NAME, 
    A.AUTHOR_NAME, 
    C.NO_OF_COPIES, 
    L.BRANCH_ID, 
    L.BRANCH_NAME, 
    L.ADDRESS
FROM BOOK B
JOIN BOOK_AUTHORS A ON B.BOOK_ID = A.BOOK_ID
JOIN BOOK_COPIES C ON B.BOOK_ID = C.BOOK_ID
JOIN LIBRARY_BRANCH L ON C.BRANCH_ID = L.BRANCH_ID;

-- 2. Get the particulars of borrowers who have borrowed more than 3 books from Jan 2017 to Jun 2017.
SELECT CARD_NO 
FROM BOOK_LENDING
WHERE DATE_OUT BETWEEN TO_DATE('01-JAN-2017', 'DD-MON-YYYY') 
                    AND TO_DATE('30-JUN-2017', 'DD-MON-YYYY')
GROUP BY CARD_NO
HAVING COUNT(*) > 3;

-- 3. Delete a book in BOOK table and update the contents of other tables accordingly.
DELETE FROM BOOK WHERE BOOK_ID = 3;

-- Since ON DELETE CASCADE is set, related entries in BOOK_AUTHORS, BOOK_COPIES, and BOOK_LENDING will be automatically deleted.

-- 4. Partition the BOOK table based on year of publication.
-- Creating a partitioned table (only possible at table creation)
CREATE TABLE BOOK_PARTITIONED (
    BOOK_ID INTEGER PRIMARY KEY,
    TITLE VARCHAR2(20),
    PUB_YEAR INTEGER,
    PUBLISHER_NAME VARCHAR2(20),
    CONSTRAINT FK_PUBLISHER FOREIGN KEY (PUBLISHER_NAME) REFERENCES PUBLISHER(NAME) ON DELETE CASCADE
)
PARTITION BY RANGE (PUB_YEAR) (
    PARTITION P1 VALUES LESS THAN (2010),
    PARTITION P2 VALUES LESS THAN (2015),
    PARTITION P3 VALUES LESS THAN (2020),
    PARTITION P4 VALUES LESS THAN (2030)
);

-- Demonstrate partitioned query (fetching books published after 2015)
SELECT * FROM BOOK_PARTITIONED WHERE PUB_YEAR >= 2015;

-- 5. Create a view of all books and their number of copies currently available in the Library.
CREATE VIEW V_BOOKS_AVAILABLE AS
SELECT 
    B.BOOK_ID, 
    B.TITLE, 
    SUM(C.NO_OF_COPIES) AS TOTAL_COPIES
FROM BOOK B
JOIN BOOK_COPIES C ON B.BOOK_ID = C.BOOK_ID
GROUP BY B.BOOK_ID, B.TITLE;
